@model IEnumerable<Examen01.Models.Preguntas>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Examenes.CodExamenes)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Pregunta)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Examenes.CodExamenes)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Pregunta)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdPreguntas }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdPreguntas }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdPreguntas })
        </td>
    </tr>
}

</table>
